---
name: siuyu-lightbox-still-life
description: Use when a user wants overhead light-table research still lifes or material studies from supplied subjects and references. Compose the subject with related studies, samples or purposeful props, using tangible objects and transmitting layers. Preserve source identity, design credible models when needed, compile the direction and inspect the actual image. Follow explicitly requested hero photographs, actions, close-ups, offline composition or renderer development through the relevant workflow.
---

# Siuyu Lightbox Still Life

把用户的主体与参考转化为可信的光台实物摄影。先按当前请求确定要呈现的对象和载体，再在这份实体范围内选择识别特征、尺寸、造型和工艺。模型可以微缩、写实、概括、夸张、彩绘或采用透明材料；所有物件具有可信构造、材料、尺度、接触和摄影表现。

## 默认视觉目标

简短要求使用本 Skill 生成图片时，默认交付**垂直俯拍的光台研究静物**，整体画面同时呈现：

- **主体与研究的对应**：图稿、材料样片或有具体用途的道具展开主体的一项可见特征，通过形状、纹样、材料或摆放让联系可辨。
- **透光与实体的层次**：底光穿过具体材料，边界与交叠表现透射密度，实体保留自身表面、暗部和接触。
- **完整的共同机位**：从承载面正上方看清主体与相关材料的分组、间距、遮挡和观看顺序。

根据主体选择研究语言、搭配、密度和配色；参考物件提供方法与关系，具体物件由本轮设计。将这三项落实为本图可见的物件和关系，再细化模型造型与工艺。

先依据参考在强透光平铺与柔光纸层档案之间选择光线方向。用户明确选择单件主视觉、动作、材质特写、完整照片或活动场景时，按该目标执行。主体模型的造型、工艺与摆放服务于当前光台构图；来源照片提供身份与结构依据。

## 画幅、桌面与现实结构

- 默认横幅采用 16:9；需要竖幅时采用 9:16。用户指定的其他比例或精确尺寸优先。横竖分别安排主体、图稿、材料与留白，生成记录填写实际原生像素。
- 默认让同一连续干净的台面铺满四边与四角。主体及必要研究物件保留完整可读边界和桌面余量；动作场景按实际握持位置安排手腕进入。特写取景按用户明确选择的重点确定。
- 器物题材先明确现实类别、功能部件、轮廓、开口、厚度和连续连接，再选择工艺与表面。例如咖啡勺有凹勺碗、收窄勺颈与圆端长柄；玻璃杯有杯口、容腔、杯底和相接的把手。模型的造型转译继续依据当前来源和用户要求。
- 整幅画面先建立主次：主体的面积、明暗或反射成为观看重点，图稿和样片分别解释具体特征；间距、错位轴线与留白服务这组关系。构图、结构和材料分别进入核心视觉复查。
- 柔光箱方向让乳白扩散面连续覆盖取景，通过单层纸、叠层密度、跨越纸边界的下层笔迹与实体接触显示透光关系。见 [柔光箱横竖示例](references/example-gallery.md#example-09)。

## 主体范围的默认选择

- 用户上传照片并简短要求“生成光台档案图”时，默认从照片中选择主体对象，按其固有结构建立台面静物；生命题材或大型对象可设计为模型。主体的穿着、实际装配附件与容器内容分别按来源中的归属判断。先写出有限的实体及部件清单，再选择模型的艺术表达。
- 模型、新绘制的主体研究图和造型样片共用这份部件清单，展开轮廓、结构、姿态或表面研究。材料样片比较当前选用制作材料的厚度、肌理或透射，采用条片、色片等有明确比较用途的形状。完整现场图面与活动场景按用户明确指定的目标呈现；原物及现成模型按其保真要求保留。
- 自主设计负责范围内的造型、材料、尺度、配色和有独立用途的搭配。识别特征从已确定的主体部件中提取；部件范围由来源结构与任务决定。具体判断见 [来源关系与呈现范围](references/object-relations.md)。
- 在 `decisions.content_scope` 记录采用范围；简短生成请求使用 `subject_study`。每个候选的 `source_relations` 按所表现内容填写原始类别，涵盖该候选的全部内容。归类贯穿模型、图稿、造型样片和独立道具；新载体、摆放位置与制作材料共同实现已经成立的内容范围。编译前逐项对照原始请求和来源证据。
- 新任务从本轮输入和 Skill 包内的字段示例建立方向。历史成图、方向和评审仅在用户指定复用，或本任务延续同一已确定版本时读取；先完成当前来源判断，再按确认范围采用历史内容。

范围判断先写所表现的对象及其固有部件。每个模型部分的归属都以它表现的内容为依据，制作材料、连接方案和定格形式在范围成立后选择。具体依据见 [来源关系与呈现范围](references/object-relations.md)。

将边界写成正面的可见要求。例如，张口的狮首鱼身雕塑以眼鼻、上下唇、口腔、鬃毛、鳞片、前鳍和卷尾构成主体；实体在口沿结束，口前连续露出光台底面。研究图采用相同轮廓，口沿外露出纸面；样片研究这些表面或所选制作材料。把这组边界同时写入 `target_scope`、构图和范围核心检查，生成后逐处核对。

识别时先区分**对象本体、实际附件、独立承托、产生的现象与图面内容**。逐个检查会改变主体轮廓的区域，依据材质边界、接合方式与作用过程判断关系，再形成模型部件清单。名称、用途和视觉显著程度用于辨认对象；部件归属采用这份关系判断。

## 执行顺序与完成条件

先确定当前使用环境：

- **Codex 桌面端、Codex CLI、IDE 扩展：** 具备本地文件、Python、图片查看与图像工具时，使用下方完整流程，保存方向、编译任务和实际生成记录。安装见 [Codex 安装](README.md#codex-安装)，终端调用见 [Codex CLI](CLI.md#codex-cli)。其他 Agent 按相应工具接口适配。
- **ChatGPT 网页普通 Chat，用户通过链接、粘贴或上传文档使用：** 先读 [ChatGPT 网页 Chat 创作说明](CHATGPT.md)。沿用主体、构图、材料、光线与看图要求，以当前对话能实际执行的方式整理提示词和生成图片。该路径可以直接使用自然语言提示词，方向文件与 Python 编译步骤按可用能力执行。
- 用户只要提示词时，交付所需文本；实际生图需要当前对话提供图像工具。记录和说明准确对应已读取的资料、已执行的步骤与真实图片。

**查看来源中的对象与关系 → 确定视觉目标与内容范围 → 组织整幅画面的物件关系 → 细化模型、材料与摄影 → 编译成像 → 对照任务检查整体目标和来源范围，再检查实物表现。**

- 默认相机在承载面正上方，光轴垂直于台面，传感器平行台面。构图与摄影字段描述同一个机位；主体通过摆放与稳固承托向镜头展示识别面。用户明确指定机位时按其要求执行。
- 先读 [全场景实物真实性](references/physical-realism.md)。主体身份、指定载体、画幅与完整构图、机位、构造、材料及物理关系都属于核心要求；核心项逐项成立后才判定成图达到要求。
- 需要新建模型、转换载体或重做构造时，读 [模型尺度与建构](references/model-construction.md) 和 [主体与建构审查](references/scene-context.md#主体与建构审查)。识别要求、造型取向、缩尺、实体尺寸、制作工艺和材料一起确定；画面中的放大程度另由摄影决定。
- 完整脚本流程每轮成像保存当前版本的 `direction.json`，实际运行 `scripts/build_image_job.py`，原样使用返回的提示词与参考顺序调用图像工具。方向变动后重新编译。网页版对话流程依照 [ChatGPT 网页 Chat 创作说明](CHATGPT.md) 组织实际提示词与附件顺序。
- 方向、审查和执行记录属于工作文件。画面文字仅承载用户指定或本轮授权创作的场景内容，并落在明确的纸张、包装或其他表面上。

## 按交付目标选择流程

| 用户要什么 | 执行方式 |
|---|---|
| 生成图片、把素材做成参考效果、修好现有画面 | 默认采用下方图像成片流程，实际调用内置图像工具。 |
| 只分析、只写提示词、只修改 Skill | 完成所请求的分析或文件。Skill 维护以修改、静态校验和同步为完成条件；图像测试按用户在维护任务中明确批准的范围执行。 |
| 明确要求完全离线、确定性导出、精确源图层合成 | 读取 [配套渲染器工作流](references/offline-workflow.md)，使用已有本地渲染器工程。 |
| 明确要求开发或修复本地渲染器 | 读取 [本地渲染器工程开发](references/codex-execution.md) 和用户指定任务。 |

图像成片流程可在普通工作目录执行。仓库、Node、Blender、SceneSpec 与工程 readiness 检查按对应工程任务使用。

“继续工作”等延续性要求沿用已经确定的任务类型与测试分工。用户安排另开对话测试时，维护任务在文件校验、同步和交付后完成。

## 1. 查看本轮输入并绑定身份

先实际打开本轮提供的每张图片，按上方默认选择和 [来源关系与呈现范围](references/object-relations.md) 写出对象、载体及部件范围。首次上传与多轮编辑都先保存这份来源判断；需要复用历史内容时，再按上方条件读取。以实际查看的文件建立 `inputs`，每条写 `id`、`path`、`role`、`observation` 和 `use`；内容输入先写 `content_analysis`，再提取 `preserve`。

- `observation` 写这个文件里能直接看见的对象与辨识差异；多主体优先记录足以区分彼此的轮廓、结构、色区或姿态。
- `content_analysis` 分别写来源中的 `relationships`、本轮的 `target_scope` 和范围依据 `basis`。`relationships` 用“对象或区域、关系类别、可见证据”列出识别结果；组成、装配、承托、容纳、产生和图面呈现分别判断。`target_scope` 从已确认的本体和附件选择模型部件，独立物件与指定场景各说明采用依据；材料和承托随后设计。
- `preserve` 从同条观察按本轮呈现方式提取约束。原物记录其结构与外观；新建模型记录足以辨认对象的特征组合和用户明确锁定项。模型的比例、姿态、材料与颜色转译写为当前设计，并说明它们怎样保持所需辨识。
- `subject` 提供主体身份；`support` 提供实际采用的内容；`reference` 提供本轮采用的光、色或布局关系；`edit_target` 指向一个具体编辑目标。
- 同一来源可有多种用途，但每种用途都在对应记录中说明。材料规格采用用户说明，照片外观采用可见证据，本轮新建材料记为设计。
- 用户只提供风格参考并要求新概念时，主体身份来自文字任务，按 `concept` 执行。

多轮任务按 [旧方案的本轮复核](references/scene-context.md#旧方案的本轮复核)，先从原始来源与当前要求重建身份依据，再决定旧方案的哪些部分仍适用。用户明确确认的构图、外观或编辑范围各按其实际含义保留；主体范围、工艺和承托分别核对。把复核后的保留项、参考用途和选择理由写入当前方向，随后编译。

把 `inputs` 当作唯一本轮输入映射，其他字段通过它的 ID 引用。重新上传素材后，依据新文件重建观察与保留项。完整观察、归属分析与选择依据保存在方向记录中；提示词中的 `use`、`target_scope`、`preserve` 及选中方案只描述本轮要呈现的内容。编译后把 `Image N` 的对象描述与 `referenced_image_paths[N-1]` 对照，实际打开有疑问的路径核对。来源观察与该文件身份一致，才进入成像。

## 2. 确定整幅画面与物件做法

按 [场景状态与情境搭配](references/scene-context.md) 先确定用户要呈现的对象范围，再决定它以原物、模型、照片或图稿的哪种形式进入场景。

读取 [图像美术指导](references/image-art-direction.md)，先用现有方向字段建立整幅画面：`intent` 写交付目标及采用依据，`design.idea` 和 `pairings` 写主体与研究或搭配材料的具体联系，`design.composition` 写全场分组、摆向与层次。默认方向同时落实上方三项视觉目标；其他方向依据用户的明确选择。开放设计比较的候选共同服务这一目标，选中后再细化各件物品。

- 原物及用户以原物身份提供的既有模型，按来源保留可见结构、颜色、材料与工艺。来源只展示局部时，明确能确认的范围。
- 人物、动物等生命题材作为台面立体主体时，默认设计为实体模型。缩尺、写实或夸张程度、体块、姿态、配色和材料由当前识别目标与画面主张联合选择。植绒、打印彩绘、玻璃、陶瓷、纸塑及其他工艺按具体形态使用，用户明确指定的要求优先。
- 先按真实手办或造型摆件的成品尺寸选择模型的高、宽、长，再按需要计算相对原型的缩尺；尺寸选择与计算由操作者完成。细节与材料厚度跟随模型物理尺寸和工艺，画面占比另由摄影决定。纸张、工具与手按各自实物尺寸参与拍摄，详见 [手办尺寸选择](references/model-construction.md#手办成品尺寸与缩尺)。
- 新建模型先选择能建立辨识的特征与造型，再用主要体块组织轮廓和重心，细化次级体量、连接、厚度、支撑、表面与涂装。所选造型的概括、夸张和材料转译都可以直接体现在主体模型上。
- 单体模型先按稳定结构确定所指对象的固有部件，再选择造型和工艺。石雕以雕刻本体的体量、相连构件、纹样和基座归属为依据；其他对象采用同样的本体识别方法。材料设计服务于这份范围，辅助物件各有自己的身份、选择理由与承托；图面与复合场景按各自选定范围表达。
- 在 `decisions.subject_basis` 写所指对象、识别要求、实体范围与造型选择，在 `scene.carrier` 和 `support` 逐件写制作方式、缩尺或设计尺寸、朝向和承托，在 `materials` 写各部位的构造、材料及颜色转译。

完成 [主体与建构审查](references/scene-context.md#主体与建构审查) 后再编译。先对照原图与任务检查内容关系及范围依据，再检查制作方案；操作者亲自完成也须用同一标准，并如实记录审查方式。

## 3. 细化搭配、全场景实物与摄影

沿用已确定的整幅画面，细化物件、材料与摄影，使各部分共同支持同一主张。

- 自主发展图稿、速写、结构或解构表达时，读 [从主体发展结构](references/subject-structure.md)。每张图稿要与当前主体有可见对应，并具有具体纸面与绘制方式。
- 情境搭配按 [场景状态与情境搭配](references/scene-context.md) 发展。开放设计简短比较至少两个不同联系；用户指定方向或局部编辑可直接记录既定选择。选中项绑定到 `pairings[].decisions`，说明依据、可见贡献和实物承载。
- 按 [材质与配色判断](references/material-color.md) 覆盖整场物件：主体、模型、纸张、图稿、承托、工具和其他道具。每类写材料、厚度或边缘、尺度、接触与光响应，外观确有差异的部位再单列。
- 在垂直俯拍下决定分组、主体摆向、取景、视觉重量、色场和留白。大形体与实体关系成立后细化纹理；常规位置、搭配和工艺按已有授权自主选择。
- 配色说明主体依据、色相关系、明度与饱和度、面积分配和环境反射。原物按来源色呈现；新建模型按识别目标选择写实涂装、色区概括、单色或材料本色，用户明确的色彩要求逐项落实。
- 逐物件按 [全场景实物真实性](references/physical-realism.md) 检查形体、材质、尺度、承托与光影，并将本图最容易失真的位置写成具体复查目标。

按 [摄影设定](references/photography.md) 选择预设并覆盖：强透光多物平铺、柔光纸层档案、大色块主视觉、工作台动作或材质近摄。用户提到柔光箱、硫酸纸、描图纸或参考突出乳白光面与纸层时，读 [柔光纸层档案](references/diffused-paper-archive.md)，采用 `diffused_paper_archive`。

配套 [示例图库](references/example-gallery.md) 提供各预设、方向候选和纸层变体的实际成图。按当前目标选择相关图片，观察机位、材料与物件对应；主体身份和内容范围继续从本轮输入建立。

每幅图确定画幅、景别、垂直俯拍下的取景高度、镜头、曝光、白平衡、灯光、对焦与动静处理。物件有明显高差、细节占比不足或涉及动作时，读 [取景与清晰范围](references/camera-framing.md)。需要设计反射、透射或纹理照明时，读 [材质目标与光路](references/lighting-optics.md)。

摄影数值是成像模拟设定，写出它们服务的可见效果。材料、光路、曝光和必要清晰范围共同支持同一场景。真实拍摄参数、具体型号与测量结果另按实际证据记录。

## 4. 编译当前方向并实际成像

在具备文件和 Python 执行能力的环境中，按 [Python 方向编译与成像调用](references/prompt-execution.md) 完成方向记录，再在本地终端运行。`<skill-dir>` 替换为实际 Skill 安装目录：

```bash
python3 <skill-dir>/scripts/build_image_job.py /path/to/direction.json --output /path/to/image-job.json
```

检查编译结果中的输入身份与顺序、载体与构造、全场景材料、机位和复查目标是否相容；修正决定时回到方向记录再编译。编译器验证内容范围与候选类别的相容性、观察字段、引用、选择覆盖和参数传递；类别与依据是否真实、具体文字是否遵循范围，仍须对照原始请求和来源逐项判断。完整场景或完整图面的采用依据须对应用户明确要求的内容；编译通过后再进行成像。

使用当前可用内置图像工具。提供 `imagegen` Skill 时，读取其当前规则；本地参考先查看，调用原样使用编译后的提示词与参考顺序。环境参数不同只转换传输字段。生成记录指向实际使用的任务文件与图片。

正常图片任务取得真实图片产物后才完成成像环节。用户指定 API、CLI 或特定服务时，执行其已授权路径；工具不可用或失败时如实记录状态并保留已有产物。

## 5. 看图、判定与修正

把成图、实际来源和选定参考同时打开。先在交付尺寸检查整体，再把对应部位放到相近尺度观察，按 [全场景实物验收](references/physical-realism.md#成图判定) 逐项记录 **位置、所见、要求、结论**。

先仅依据原图与用户请求重新列出主体部件、图面范围和应有的视觉目标，再与方案和成图逐项比较。把来源范围与视觉目标分别列为核心项：前者逐件核对采用依据，后者核对俯拍、光台材料层次及主体与研究材料的对应，或用户另行明确的画面目标。要求来自用户任务、原物结构或上述默认选择。在交付尺寸核对主体是否首先可辨、所有必要轮廓是否完整、连续桌面和留白是否成立，再检查本轮模型的尺寸、造型、工艺、材料和全部物件的接触关系；细部精修按其实际影响安排。物件可见侧面须结合摆向判断相机；软光下接触影可浅，接触、遮挡和高差仍应连贯。

核心项使用 `PASS`、`REVISE` 或 `UNVERIFIED`：可见证据满足要求才通过；有矛盾就修正；关键处看不清则标记未确认。总体状态按 [验收记录与状态汇总](references/physical-realism.md#验收记录与状态汇总) 由核心项统一计算，并运行记录校验。所选工艺与可见连接属于核心要求；交付说明、评审与生成记录使用同一结论，停止成像时仍保留实际视觉状态。

- 搭配、构图、载体、体块或制作结构重建：使用 `recompose`，以原始身份资料重组；仍适用的布局关系按本轮采用范围保留。
- 已成立结构中的局部材料、光照或连接细化：使用 `detail_edit`，绑定当前图片，明确改变的部位与保留项。
- 用户要求新概念：使用 `concept`。

先处理最影响真实性或任务成立的决定。首次探索通常生成一个方向；确需比较结构时可生成两个。默认一轮比较后最多进行两次有明确目标的修正，用户范围优先。达到上限仍有核心项待解决时，交付当前版本并说明具体未达标处与下一动作。实际调用、视觉评估和用户审美接受分别记录。

## 6. 交付与维护

先展示图片，再简述方向、版本化工作区路径和实质限制。保留图片、实际提示词及简短生成与看图记录；工程控制文件和完整报告按用户需求提供。已有文件采用新增版本保存。对外发布与用户审美接受按对应授权和证据处理。

- [评估用例](evals/evals.json) 提供行为场景；实际执行证据按各次运行记录。
- `scripts/validate_skill.py` 检查包结构、链接、样例和编译器可执行性。
- 配套渲染器的 schema、审批与 `@lightbox/cli` 从 [本地渲染器工作流](references/offline-workflow.md) 按需进入；各类命令的执行环境见 [命令行说明](CLI.md)。
